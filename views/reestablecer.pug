<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 #{nombrePagina}
            p Agrega tu e-mail para reestablecer tu cuenta

            if locals.mensajes
                .alerta
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p #{error}

            form.caja-login(method="POST" action="/reestablecer")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Correo electrónico" required value= email)
                .campo.enviar
                    input(type="submit" value="Reestablecer contraseña").boton
                .campo.acciones
                    a(href="/iniciar-sesion") Ya lo recordé, volver al login